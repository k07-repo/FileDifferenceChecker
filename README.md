# FileDifferenceChecker

Personal use project to check all differences between two folders.
Something like a GUI tool version of the "diff" command, but for Java GUI although it's not perfect (yet, at least).
